//Def var1 = 4*T;
//For T from -10 to 10 step 0.2 draw(T*2,aver());
//For T from -10 to 10 step 0.2 draw(T*2,max(3*T,T**2));
For T from -10 to 10 step 0.2 draw(T*2,aver(3*sin(T*2),T**2,-exp(T)));
//Let var2 = 12/T;
//ROT is PI/4;
//Def quadratic = T**2-5*T-3;
//For T from -10 to 18 step 0.2 draw( T/5 , quadratic/10 );

//For T from -10 to 10 step 0.2 draw(T,6*tan(T/4));

//For T from -10 to 10 step 0.2 draw(T,tan());
